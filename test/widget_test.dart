import 'package:flutter_test/flutter_test.dart';
import 'package:ctos_companion/app.dart';

void main() {
  testWidgets('App class exists', (WidgetTester tester) async {
    expect(CtosApp, isNotNull);
  });
}
